return {
  "folke/snacks.nvim",
  opts = {
    explorer = {
      -- This shows hidden files (dotfiles) by default
      hidden = true,
      -- This also shows files ignored by git (optional)
      ignored = true,
    },
  },
}
